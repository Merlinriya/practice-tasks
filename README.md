git init
git config --global username
git config --global email

["create a file"]

git add --filename (or) git add . --to add all the files
git status
git commit -m "your comments here"
git push ["give username and password]

BRANCHES

git branch
git checkout  (branch name)
git push -u origin (branch name) --to push from branch
git merge (branch name) --to merge the branches
git pull
git pull origin main --to get all the datas of main to branch
